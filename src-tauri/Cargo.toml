[package]
name = "mission-control"
version = "0.1.0"
description = "A local canvas for orchestrating Claude Code agents"
edition = "2021"

[lib]
name = "mission_control_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
